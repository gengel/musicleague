node_modules
*.local
.DS_Store

# Never commit a league export: it holds real names and every vote cast.
# src/data is where the working copy of an export lives.
*.csv
src/data/
.cache/

# Snapshots hold a full copy of the export for whatever round they archive —
# same reasoning as src/data/ above. They're for local/host backup, not git.
snapshots/
